# Filtering on package and module
{
  transactionBlocks(
    filter: {
      function: "0x3::mys_system"
    }
  ) {
    nodes {
      sender {
        address
      }
      gasInput {
        gasPrice
        gasBudget
      }
    }
  }
}
