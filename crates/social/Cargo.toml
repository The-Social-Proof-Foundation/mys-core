[package]
name = "social"
version = "0.1.0"
edition = "2024.beta"
authors = ["The Social Proof Foundation"]
description = "Social networking features for the Mys blockchain"
license = "Apache-2.0"
publish = false

[dependencies]
# Add dependencies as needed